# 监控设置说明

PriceAI 账号价格监控通过 Price Radar V1 公开快照获取报价数据，在本地应用筛选规则后通知用户。本文档解释设置窗口中各项参数的作用。

Price Radar 每条报价的示例结构：

```json
{
  "id": "id-rmfvli",
  "title": "GPT plus印度upi已接码成品号(带RT)",
  "price": 11.33,
  "currency": "CNY",
  "status": "low_stock",
  "effective_status": "available",
  "stock_count": 3,
  "captured_at": "2026-07-22T02:48:28.418+00:00",
  "last_seen_at": "2026-07-22T02:48:28.418+00:00",
  "verified_at": "2026-07-22T02:48:28.418+00:00",
  "expires_at": "2026-07-23T02:48:28.418+00:00",
  "url": "https://pay.ldxp.cn/item/1n4kbh"
}
```

---

## 监控条件

### 最高监控价格（`max_price`）

报价 ≤ 此值时才匹配。

Price Radar 返回的 `price` 字段表示商家标价（单位：元）。设为 10 表示只关注 10 元及以下的报价。高于此值的报价会被忽略。

### 最低价格（`min_price`）

报价 < 此值时排除，用于过滤异常低价。

卡网偶尔会出现 0.01 元等明显不合理的报价，通常是无实际库存或标价错误。设为 0 表示不启用此过滤。

### 最低库存（`min_stock`）

报价的 `stock_count` 必须 ≥ 此值才匹配。

API 返回的每条报价带有 `stock_count`（库存数量）和 `status`（库存状态）。程序同时检查：

- `status` 必须为 `in_stock`（有货）或 `low_stock`（库存紧张）
- `effective_status` 不能为 `unavailable`、`stale` 或 `failed`
- `stock_count` 不能为 `null`（库存未知也会被排除）
- `stock_count` 必须 ≥ 最低库存值

默认值为 1，即只要有至少 1 件库存就匹配。设为更高的值可以只关注库存充足的报价。

### 报价有效时间（`fresh_within_minutes`）

只接受最近 N 分钟内被采集或验证过的报价。

每条报价带有以下时间戳（按优先级取第一个非空值）：

1. `verified_at` — 报价验证时间
2. `last_seen_at` — 最后一次采集到该报价的时间
3. `captured_at` — 报价首次采集时间

程序计算该时间与当前时刻的差值，超过 N 分钟的报价视为过期，不会被匹配。

**作用**：卡网库存变化很快，一条报价可能在几分钟后就售罄或下架。此设置确保只提醒近期仍然有效的报价，避免看到低价但实际早已没货的情况。

默认值为 120 分钟。设为 0 表示不限制（接受所有时间段的报价）。

### 刷新间隔（`check_interval_seconds`）

持续监控模式下，每 N 秒检查一次 Price Radar 快照。

PriceAI 官方要求轮询间隔不短于 60 秒。程序会读取 `latest.json` 指针，只有 `snapshot_id` 变化时才下载新的不可变快照，因此即使设置较短的间隔也不会产生过多流量。

建议值：120～300 秒。快照本身约 5 分钟刷新一次，设置更短的间隔只会增加空轮询次数。

---

## 关键词过滤

### 包含关键词（`required_keywords`）

报价的标题、商家名和渠道名必须**同时包含**所有指定的关键词才匹配。留空表示不限制。

### 排除关键词（`excluded_keywords`）

报价的标题、商家名和渠道名中**包含任意一个**指定的关键词就会被排除。留空表示不排除。

关键词不区分大小写，可用换行或逗号分隔输入多个。

---

## 通知设置

### Windows 桌面通知（`windows_toast`）

启用后，匹配到合适报价时会弹出 Windows Toast 桌面通知。关闭后仅在控制台/运行日志中显示。

### Webhook（`notifications.webhook_url`）

填写 URL 后，匹配到报价时会向该地址发送 JSON POST 请求，包含 `title`、`message`、`offers` 字段。适合接入自定义通知服务。

### Telegram（`notifications.telegram`）

填写 `bot_token` 和 `chat_id` 后，匹配到报价时会通过 Telegram Bot 发送消息。

---

## 数据来源说明

- 快照指针：`https://data.priceai.cc/latest.json`
- 不可变快照：指针中 `snapshot_url` 字段指向的地址
- 刷新频率：约 5 分钟
- 认证：无需 API Key
- 缓存：支持 ETag，`snapshot_id` 不变时无需重新下载快照
- 排序规则：有货报价优先，拼车/团购、镜像站、网页号、Telegram Stars 不参与默认排名

Price Radar 只提供购买前参考，不保证第三方报价仍然有效。实际购买前请回到原站确认价格、库存、交付方式和售后规则。
