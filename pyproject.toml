[project]
name = "priceai-monitor"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.3.0",
    "pystray>=0.19.5",
    "requests>=2.34.2",
    "windows-toasts>=1.3.1",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.22.2",
]
